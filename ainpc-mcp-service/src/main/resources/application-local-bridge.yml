mcp:
  snapshot:
    path: data/mcp-runtime-snapshot.json
    cache-ttl-seconds: 2
    stale-threshold-seconds: 60
    blocklist-fields: ""
  command:
    path: data/mcp-commands
  audit:
    path: data/mcp-write-audit.json
    max-entries: 1000
